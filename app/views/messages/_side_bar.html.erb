<div class="side-bar-header">
  <div class="header-name">
      <%= link_to current_user.name, edit_user_path(current_user)%> <%# パスにidが入っているときはidの情報が入っているインスタンスを引数として渡してあげることにより指定 %>
    <%# link_to         テキスト              リンク先のパス %>
  </div>
  <div class="create-room">
    <%= link_to "チャットを作成する", new_room_path %>
  </div>
</div>

<div class="rooms">
  <% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <a href="#"><%= room.name %></a>
      </div>
    </div>
  <% end %>
</div>